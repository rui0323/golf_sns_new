<div class="container mt-5 mb-5">
<table>
    <thead>
        <tr>
            <th><h3>投稿者名</h3></th>
            <th></th>
            <th><h3>タイトル</h3></th>
            <th></th>
            <th></th>
        </tr>
    </thead>

    <tbody id="post">
      <% posts.each do |post| %>
        <tr>
          <th><h3><%= post.customer.name %></h3></th>
          <th></th>
          <th><h3><%= post.title %></h3></th>
          <th><h3><%= link_to "詳細", post_path(post) %></h3></th>
          <% if post.customer == current_customer %>
          <% else %>
       
        <% end %>
      </tr>
    <% end %>
    </tbody>
</table>
</div>