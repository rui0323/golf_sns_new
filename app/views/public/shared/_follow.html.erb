
             <% if current_customer.following?(customer) %>
             <%= link_to "フォロー中", customer_relationships_path(customer.id), class: "btn-gradient-simple mx-auto my-1", method: :delete,remote: true %>
             <% else %>
             <%= link_to "フォローする", customer_relationships_path(customer.id), class: "btn-gradient-simple mx-auto my-1", method: :post,remote: true %>
             <% end %>